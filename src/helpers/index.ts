// import { proxyRequest, useProxy } from './proxyRequest';
import { cache } from "./mongooseCache";
import {google} from './google';

export const helpers = {
    cache,
    google,
    // proxyRequest,
    // useProxy
}